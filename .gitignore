# Raw partition images (too large for GitHub — keep locally)
prebuilt/boot.img
prebuilt/vendor_boot.img
prebuilt/tmp/

# Individual .ko files (referenced via modules.load, not committed directly)
prebuilt/modules/*.ko

# OTA work directory (local extraction workspace)
ota_work/

# macOS
.DS_Store
**/.DS_Store

# Editor
.vscode/
*.swp
*~
